crate:
	Inherits: ^CoreVisibleActor
	HitShape:
		Type: Circle
			Radius: 512
	Tooltip:
		Name: Resource Crate
	Health:
		HP: 150
	ResourceCrate:
	Interactable:
	Targetable:
		TargetTypes: Ground
	Mobile:
		Locomotor: crate
	WithFacingSpriteBody:

# Crate special locomotor as it can not move itself, but is movable by other actors.
^CoreWorld:
	Locomotor@crate:
		Name: crate
		TerrainSpeeds:
